# log4j2-async-spring-boot-restart-issue

gw clean build dBI

docker network create pbrowngh

docker-compose -f docker-compose.yml up -d configserver discoveryserver sampleserver

docker logs -f <sample-server-container-id>
